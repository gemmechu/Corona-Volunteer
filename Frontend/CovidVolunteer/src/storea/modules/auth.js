
const state = {
  auth: [
    {
      id: '2',
      email: '',
      password: '',
    },
  ],
}

const getters = {
  myauth: (state) => state.auth,
}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations,
}
